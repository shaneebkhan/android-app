<odoo>
    <data>
        <!-- assets -->
        <template id="assets_frontend" inherit_id="website.assets_frontend" name="Privacy assets frontend">
            <xpath expr="." position="inside">
                <link rel='stylesheet' href='/website_privacy/static/src/less/website_privacy.less'/>
                <script type="text/javascript" src="/website_privacy/static/src/js/website_privacy.js"/>
            </xpath>
        </template>

        <!-- cookies bar -->
        <template id="cookies_bar" inherit_id="website.layout" name="Privacy Cookies Bar">
            <xpath expr="//footer" position="after">
                <div class="cookie_bar" t-if="website.cookies_bar and not request.httprequest.cookies.get('cookies_consent')">
                    <div t-field="website.cookies_bar_msg" class="table-cell"/>
                    <div class="table-cell">
                        <a class='btn_cookies btn btn-sm btn-primary' href="#" t-field="website.cookies_bar_btn"/>
                    </div>
                </div>
            </xpath>
        </template>

        <!-- signup consent -->
        <!-- <template id="signup_consent" inherit_id="auth_signup.fields" name="Privacy Signup Consent">
            <xpath expr="//div[contains(@class, 'field-confirm_password')]" position="after">
                <div class="privacy-consent mb16">
                    <input type="checkbox" id="privacy-content-chkbx" class="privacy-content-chkbx pull-left mr8" required="required" t-if="website.signup_force_checkbox" />
                    <label for="privacy-content-chkbx" t-field="website.signup_consent" />
                </div>
            </xpath>
        </template> -->

        <!-- Google Analytics -->
        <template id="google_analytics_anonym" inherit_id="website.layout" name="Privacy Google Analytics Anonymized">
            <xpath expr="//script[@id='tracking_code']" position="inside">
                <t t-if='website.ga_anonymize_ip'>    ga('set', 'anonymizeIp', true);</t>
            </xpath>
        </template>


    </data>
</odoo>