<odoo>
    <template id="report_package_barcode_delivery" inherit_id="stock.report_package_barcode">
        <xpath expr="//div[hasclass('o_packaging_type')]" position="after">
            <div t-if="o.shipping_weight" class="col-auto mw-100 mb-2">
                <strong>Shipping Weight:</strong>
                <br/>
                <span class="m-0" t-field="o.shipping_weight"/>
                <span t-esc="env['product.template']._get_weight_uom_id_from_ir_config_parameter().name"/>
            </div>
        </xpath>
    </template>

    <template id="report_package_barcode_small_delivery" inherit_id="stock.report_package_barcode_small">
        <xpath expr="//div[hasclass('o_packaging_type')]" position="after">
            <div class="row o_package_shipping_weight" t-if="o.shipping_weight">
                <div class="col-12 text-center" style="font-size:24px; font-weight:bold;">Shipping Weight: <span t-field="o.shipping_weight"/> <t t-esc="env['product.template']._get_weight_uom_id_from_ir_config_parameter().name"/></div>
            </div>
        </xpath>
    </template>
</odoo>
