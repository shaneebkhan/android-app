<?xml version="1.0" encoding="UTF-8"?>
<odoo>
        <!-- External page : thanks message -->
        <template id="rating_external_page_view" name="Rating Page Done">
            <t t-call="web.frontend_layout">
                <div class="container">
                    <div class="text-center mt-5">
                        <h1 class="d-inline">Thank you,</h1><h3 class="d-inline"> We appreciate your feedback!</h3>
                    </div>
                    <div class="text-center">
                        <a role="button" t-att-href="web_base_url" class="btn btn-primary my-5">Go back to the Homepage</a>
                    </div>
                </div>
            </t>
        </template>

        <!-- External page: rate and submit feedback -->
        <template id="rating_external_page_submit" name="Rating Page Submit">
            <t t-call="web.frontend_layout">
                <div class="container card mt-4 o_rating_rating">
                    <div class="card-body">
                        <div clas="row">
                            <h1 class="mt-5">Thank you for rating our services!</h1>
                            <table class="mt-5">
                                <tr>
                                    <t t-foreach="rate_names" t-as="rate_name">
                                        <td>
                                            <a class="o_rating" href="#" t-att-data-rate="rate_name">
                                                <img t-attf-src='/rating/static/src/img/rating_#{rate_name}.png' t-att-alt="rate_name_value" t-att-title="rate_name_value" t-attf-class="o_rating_smily mr-md-5 #{rate == rate_name and 'o_rating_active' or ''}"/>
                                            </a>
                                        </td>
                                    </t>
                                </tr>
                            </table>
                            <div class="clearfix"></div>
                            <p class="mt-5">
                                Feel free to write a feedback of your experience:
                            </p>
                            <form t-attf-action="/rating/#{token}/submit_feedback" method="post">
                                <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                <input type="hidden" name="rate" t-att-value="rate"/>
                                <textarea class="form-control" name="feedback" rows="8" t-att-value="rating.feedback"></textarea>
                                <button type="submit" class="btn btn-primary" style="margin-top:8px;">Send Feedback</button>
                            </form>
                        </div>
                    </div>
                </div>
            </t>
        </template>
</odoo>
